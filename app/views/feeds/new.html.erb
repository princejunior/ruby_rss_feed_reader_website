<h1>New Feed</h1>
<!-- Form to create a new feed -->
<%= form_with model: @feed, local: true do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.label :url %>
  <%= form.text_field :url %>
  <%= form.submit %>
<% end %>
